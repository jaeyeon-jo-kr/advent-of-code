(ns aoc.test-day10
  (:require [aoc.day10 :refer :all]
            [clojure.test :refer :all]))

(deftest register-initial-test
      (testing "initial-register"
        (is (= register 1))))

(deftest parse-instruction-test
  (testing "parse status"))

